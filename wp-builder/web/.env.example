NODE_ENV=
PORT=

# Server-side: netmart API base for introspect.
PLATFORM_API_URL=

# Client-side (inlined into the bundle at Docker build time).
NEXT_PUBLIC_PLATFORM_FRONTEND_URL=
NEXT_PUBLIC_WP_BUILDER_API_URL=

# Nginx-proxy / similar.
VIRTUAL_HOST=
VIRTUAL_PORT=
